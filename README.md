# Lab8
The goal of this assessment is to implement a K-Nearest Neighbors (KNN) algorithm to classify data points from a provided dataset (KNN_Project_Data). The process involved feature scaling, model training, performance optimization via the Elbow Method, and final model evaluation.
